@model IEnumerable<e_CarSharing.Models.Colour>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Index";
}

<br />
<h2>Cores</h2>
<br />
<p>
    <button type="button" class="btn btn-success btn-sm" onclick="location.href='@Url.Action("Create","Colours")'">
        <span class="glyphicon glyphicon-plus"></span>&nbsp;Criar&nbsp;Nova
    </button>
</p>
<hr />

@if (Model.Count() != 0)
{
    <table class="table table-bordered table-responsive table-striped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ColourName)
            </th>
            <th>
                @Html.DisplayName("Ações")
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ColourName)
                </td>
                <td>
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.ColourId }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }

    </table>
}
else
{
    <h5>Ainda não existem cores criadas.</h5>
}

    <div>
        <button type="button" class="btn btn-secondary btn-sm" onclick="history.go(-1); return false;" href="#">
            <span class="glyphicon glyphicon-chevron-left"></span>&nbsp;Voltar
        </button>
    </div>